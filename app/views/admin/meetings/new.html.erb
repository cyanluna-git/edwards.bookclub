<section class="hero">
  <p class="eyebrow">Admin Meetings</p>
  <h1>New meeting</h1>
  <p class="hero-copy">Create the event record that will hold attendance, review text, and photo references.</p>
</section>

<section class="panel">
  <%= render "form", meeting: @meeting, submit_label: "Create meeting", fiscal_period_options: @fiscal_period_options %>
</section>
